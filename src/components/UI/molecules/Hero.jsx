import React from 'react'
import styles from '../../style'

const Hero = () => (
    <section id='home' className={`flex md:flex-row flex-col ${styles.paddingY}`}>
        <div>
            <p className='text-white leading-8'>FAST & SEEMLESS TRANSACTIONS</p>
        </div>

    </section>
    
)

export default Hero